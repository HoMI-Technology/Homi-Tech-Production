node_modules/
.next/
out/
.env
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
.vercel
.DS_Store
.env*
!.env.example

# Lighthouse CI local report artifacts
.lighthouseci/

# Playwright E2E local artifacts (reports, traces, failure screenshots)
test-results/
playwright-report/

# Claude Code local state + agent git worktrees (never commit; their __tests__
# otherwise pollute local `vitest` with phantom duplicate-file failures)
.claude/
