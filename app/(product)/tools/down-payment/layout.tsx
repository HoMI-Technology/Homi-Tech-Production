import { toolMetadata } from "@/lib/seo/tool-seo";

export const metadata = toolMetadata("/tools/down-payment");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
