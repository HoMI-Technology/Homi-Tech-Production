import { NotFoundContent } from "@/components/layout/NotFoundContent";

/** Content-only: product layout already mounts chrome and main#main. */
export default function ProductNotFound() {
  return <NotFoundContent />;
}
