# CODEOWNERS — HōMI Tech
#
# Review ownership for paths that are expensive or dangerous to get wrong.
# GitHub matches the LAST matching rule in this file, so add new, more specific
# rules BELOW broader ones. Do not delete existing rules when adding your own.
#
# These rules only take effect once "Require review from Code Owners" is
# enabled in branch protection for `main` (Settings → Branches → main).

# ---------------------------------------------------------------------------
# Section ownership (see docs/SECTIONS.md)
#
# These blocks mirror the Section Map so a PR that sprawls across Sections
# shows multiple owning lines — a fast visual signal that a task "overworked"
# beyond its Section. Today every Section resolves to @HoMI-Technology; swap in
# per-Section teams (e.g. @HoMI-Technology/finance) as they are created to make
# the boundaries enforceable per-area. GitHub matches the LAST rule, so the
# workflows rule below stays most-specific.
#
# Section 0 · Assessment & Scoring
# Only /lib/scoring/ is FROZEN (guardrail #1 — change with explicit sign-off).
# The rest is the editable layer around the engine; vertical branching runs
# through it (Plans.md Phase 5) while /lib/scoring stays byte-identical.
/lib/scoring/            @HoMI-Technology
/lib/questions/          @HoMI-Technology
/lib/validation/         @HoMI-Technology
/lib/assessment/         @HoMI-Technology
# Section 3 · AI Agents / Companion
/lib/agents/             @HoMI-Technology
# Section 4 · Money / Finance / Integrations
/lib/planner/            @HoMI-Technology
/lib/finance/            @HoMI-Technology
/lib/plaid/              @HoMI-Technology
/lib/stripe/             @HoMI-Technology
/app/api/webhooks/       @HoMI-Technology
# Section 7 · Auth / Account / Admin / Access
/lib/auth/               @HoMI-Technology
/lib/entitlements.ts     @HoMI-Technology
/app/auth/               @HoMI-Technology
# Section 8 · Platform / Shared (touch deliberately)
/lib/supabase/           @HoMI-Technology
/lib/security.ts         @HoMI-Technology
/lib/brand/              @HoMI-Technology
/middleware.ts           @HoMI-Technology
/supabase/               @HoMI-Technology
# ---------------------------------------------------------------------------

# GitHub Actions workflow definitions. Changes here can silently multiply
# billed Actions minutes, disable a required check, or remove the concurrency
# guards that cancel superseded runs — so they always need an owner's review.
/.github/workflows/ @HoMI-Technology
