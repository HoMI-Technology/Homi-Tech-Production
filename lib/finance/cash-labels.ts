/**
 * User-visible cash labels. Same words from Track vs Stand vs Plaid
 * without naming the store is a bug (homi-operate).
 */

export { PERIOD_SURPLUS_FORMULA, PERIOD_SURPLUS_LABEL } from "@/lib/finance/metrics";

/** Track planner calendar — remaining-month net, not Stand period surplus. */
export const TRACK_NET_CASH_LABEL = "Track net cash · this month";

/** Track planner ReadinessHero cash-flow tile. */
export const TRACK_CASH_FLOW_LABEL = "Track cash flow";
