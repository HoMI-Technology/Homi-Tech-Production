/**
 * SHELL_CRAFT v3 retired the left rail. Signed-in chrome is the quiet top bar.
 * Keep this alias so older imports resolve to AppHeader.
 */
export { AppHeader as AppSidebar } from "@/components/layout/AppHeader";
