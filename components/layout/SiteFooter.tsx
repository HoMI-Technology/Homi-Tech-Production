import { QuietHomeFooter } from "@/components/layout/QuietHomeFooter";

/**
 * Shared site footer. Every mount (marketing, product, not-found, share)
 * renders the quiet three-row cut. SitemapFooter is unused.
 */
export function SiteFooter() {
  return <QuietHomeFooter />;
}
