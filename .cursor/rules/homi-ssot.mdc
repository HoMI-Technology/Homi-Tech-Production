---
description: HōMI product SSOT — only this repo is product truth
alwaysApply: true
---

# HōMI SSOT

- Product source of truth is GitHub `HoMI-Technology/Homi-Tech-Production` and **this workspace only**.
- Canonical local path on the founder machine: `C:\dev\apps\homi-production` (`HOMI_SSOT`); worktrees live under `C:\dev\worktrees\homi-production--<slug>`. (Historical `C:\Users\cody\...` paths are a retired machine — ignore them.)
- Local checkouts go stale in hours — **`git fetch origin` first; base work on `origin/main`**, never on local branch state.
- Never invent a second product root under Desktop `HoMI Tech` archives.
- Prefer small PRs; after merge keep `main` 1:1 with `origin/main`.
- Agy must not write here; use EVO playground for experiments.
- Agent operating system: `.cursor/commands/homi-finish.md` (master goal+loop for the named team), `docs/knowledge/homi-agent-knowledge.md` (compiled cross-bot canon), `docs/design/baseline/` (founder-reviewed visual baselines), `docs/design/redesign-audit-seed.md` (gap seed with staleness alert).
