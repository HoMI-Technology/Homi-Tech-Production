{
  "name": "HōMI (BUILD-SAFE / FULL-STACK DEV)",
  "install": "npm ci && npx playwright install --with-deps chromium",
  "start": "bash scripts/cloud-agent-env.sh",
  "terminals": [
    {
      "name": "next-dev",
      "command": "npm run dev"
    }
  ]
}
