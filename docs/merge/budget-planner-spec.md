# HōMI Budget Planner — Visual & Functional Spec
*Distilled from 63 screenshots in `/mnt/agents/upload/*.png` (two rounds). Agents MUST read the referenced PNGs directly for pixel/copy fidelity. Verbatim UI copy is quoted; reproduce it exactly (it is the owner's product copy).*

## 0. Global shell
- Route `/planner`. In-page header block: eyebrow `DECISION READINESS INTELLIGENCE` (cyan, tracking-widest, uppercase, mono-ish small), Fraunces italic display title `Budget Planner`, tagline (rotates per state — seen variants):
  - `Cash flow, banks, portfolio, Path to Ready, and decision models — HōMI numbers for real choices, not a sales funnel.`
  - `HōMI-Score, live signals, Path to Ready, debt lab, and household dual readiness — numbers that protect decisions.`
  - `Stress patterns, protective nudges, and closed-loop score moves — numbers that protect decisions.`
  - `Closed-loop score, signals, Path, housing lens, debt lab, and household dual readiness — actions that move the number.`
  - `Your numbers. Protective nudges. Closed-loop score moves when cash, path, or stress change.` (empty state)
  - Top-right button: `Reset demo` (RotateCcw icon) when demo data loaded; `Clear data` when user/empty data. (No "Sign in" — our build has no auth.)
- Five-tab pill nav (rounded container, active tab = cyan-tinted bg + cyan text, inactive = dim): `Overview` (LayoutDashboard) · `Calendar` (CalendarDays) · `Banks & bills` (Landmark) · `Wealth` (LineChart) · `Plan` (Compass). Mobile: tabs wrap/scroll.
- Footer (every tab, verbatim planner variant): `HōMI is a product of HOMI TECHNOLOGIES LLC. Educational guidance only — not financial, legal, tax, or investment advice. Bank/broker linking, Monte Carlo, and decision models are demo flows for product validation.` Empty-state variant ends: `Your data stays on this device until you connect production services.`
- Bottom strip (Calendar & mobile views): tiles `LINKED CASH $18,720.60` (emerald) · `PORTFOLIO $57,077.95` (cyan) · `OPEN BILLS $2,493.88` (gold) + quick links `Plan` `Wealth` `Banks`.
- Refs: budget-desktop.png, budget-mobile.png, prod-final.png, readiness-desktop.png, readiness-mobile.png.

## 1. ReadinessHero (all tabs, top card)
- Left: eyebrow `HŌMI-SCORE` + verdict chip (`✦ ALMOST` emerald-outline / `✦ NOT YET` crimson / `⛨ HARD-STOP` crimson when active). Giant score numeral (JetBrains Mono, e.g. `73`) + verdict label (`Almost there` emerald Fraunces italic / `Not yet` crimson) + `of 100 · live from your numbers`.
- Summary sentence, e.g.: `Your readiness is balanced: Perfect Timing at 80/100 and Emotional Truth at 66/100 are moving together.` Hard-stop variant: `Your strongest signal is Financial Reality (51/100), but a red-line condition is active. This is a protection signal — not a judgment. Clear it first.` + crimson callout box: `Your credit score is below 620. Lenders will price this as high-risk. Build credit first; you protect yourself by waiting.`
- Three pillar bars: `FINANCIAL 74%` / `EMOTIONAL 66%` / `TIMING 80%` with cyan progress bars.
- Right: 2-col tile grid: `CASH FLOW +$3,033` (emerald, signed) · `INCOME $6,650` (emerald) · `SPENT $3,617` (dim/emerald) · `SAVED 46%` (cyan) · `BANK CASH $18,720.60` (cyan) · `PORTFOLIO $57,077.95` (cyan) · `NET WORTH $72,098.56` (cyan) · `BILLS OPEN $2,493.88` (gold) · `RUNWAY 5.2 mo` (emerald) · `DTI 3%` (emerald). Empty state: zeros + `RUNWAY ∞`.
- Refs: readiness-overview.png, prod-final.png, prod-empty.png, current-state.png.

## 2. Always-on SignalsStrip (all tabs, below hero)
- Label row: `ALWAYS-ON SIGNALS` + right-aligned `N active · stress · cash · path`.
- Horizontal cards (severity-tinted border/bg: crimson=protection/if-then, amber=pillar/stress, cyan=start), each with kind chip (`IF-THEN` / `PROTECTION` / `PILLAR` / `START` / `INDEX 60`), title, body, CTA link, × dismiss. Examples:
  - `IF-THEN — 2 overdue bills` · `TECO electric + 1 more — $260 past due. If-then: open Banking → Pay now.` · CTA `Pay bills`
  - `1 overdue bill` · `Auto insurance — $148 past due.` · `Pay bills`
  - `PROTECTION — One hard-stop is active` · `Your credit score is below 620. Lenders will price this as high-risk. Build credit first; you protect yourself by waiting. Not yet is not no — it is protection.` · `Open Plan`
  - `PILLAR — Perfect Timing is the gap` · `Perfect Timing sits at 30% of its max — the clearest place to focus next.` · `See score`
  - `INDEX 60 — Stress is climbing` · `Trend slope is +0.68 pts/day across 7 check-ins` · `Check in`
  - `START — No Path to Ready yet` · `Generate one binding-constraint sequence from your live numbers.` / empty-state variant adds `One path beats a wall of goals.` · `Build path`
- Powered by planner/signals.ts. Refs: readiness-overview.png, prod-empty.png, audit-overview.png.

## 3. Overview tab (OverviewCommand)
Sections top→bottom:
1. **Suggested move** (`✦ SUGGESTED MOVE` + right note `Protective nudges · not pressure tactics`): big card w/ kind chip (`IF-THEN`), Fraunces title `When you open Banking, pay TECO electric`, body `If-then: open bill pay → select TECO electric → Pay now. Clear the past-due loop in one sitting.`, full-width cyan CTA `Pay now →`. Side stack: `MICRO-STEP — Two-minute Path move` (`Micro-commitment: mark progress on “Optional 90-day readiness review” or skip it honestly. Open loops tax readiness.`), `PROTECT — Stress is climbing` (`Trend slope is +0.68 pts/day across 7 check-ins (now 7/10). Rising load often leads decisions. → Protect the next decision: pause big…`). Hard-stop variant: `PROTECT — Protect the decision first` + `See hard-stops →` CTA; `SYNC — Align with your partner on one number` (`Partner alignment is the soft gap. One shared number (max housing payment or runway target) beats a full budget debate.`).
2. **Path strip**: emerald bar `Path: READY band — optional maintenance only` + `Open Plan` link; or card `PATH · OPTIONAL MAINTENANCE 100%` / `Binding: READY — optional review only` / Fraunces `Path steps complete — reassess when life moves` / `Full path` link; or `PATH HABIT · FIRST MOVE — Generate a protective sequence from live numbers` / `One binding constraint at a time — not a checklist wall.` + cyan `Generate Path` button.
3. **30-day cash path** (bar chart, cumulative ledger flow, emerald positive / crimson negative bars): `30-DAY CASH PATH` / `Cumulative ledger flow · last 30 days ending demo date` / axis labels `Start $0` → `Now $3,033`. Next to it **Quick actions**: 4 tiles (`Calendar — Bills & runway`, `Plan — Path & models`, `Banks — Pay bills`, `Wealth — Portfolio`) + `NEXT FROM SCORE` list (`1. Top the emergency fund to a full 6 months.` / empty-state: `1. Rebuild credit above 660 with on-time payments and lower utilization. 2. Grow the down payment toward 10–20%. 3. Push credit above 700 for better pricing.`).
4. **START HONEST** (empty state only): Fraunces `Add real numbers — no sample data` / `HōMI scores what you enter. Link cash, log spend, schedule bills, then generate Path to Ready. Educational guidance only — not advice or a lending decision.` 4 numbered cards: `1 · Accounts — Add checking and savings balances you trust. Open Banks` / `2 · Ledger — Record income and expenses so cash flow is true. Stay on Overview` / `3 · Bills — Schedule obligations; pay them to close the loop. Open Banks` / `4 · Path — Generate protective steps from live runway, DTI, and cash. Open Plan`.
5. **Daily check-in** (`DAILY CHECK-IN` / Fraunces `Financial stress` / `Slope, level, volatility, and streaks — not a single bad day. Protective, not judgmental.`): tiles `INDEX 0` `SLOPE +0.00` `MEAN 0.0` `VOL 0.0`; `One honest score a day is enough. No judgment — just a signal when the pattern changes.`; `Log today's financial stress (1 calm · 10 overwhelmed).`; slider 1–10 labeled `1 calm` … `10 crisis` with `Today · Loaded` and `5/10`; optional input `What's driving it? (optional)`; cyan `Save check-in`; history note `No check-ins yet.` / `Last 0 days · amber bars ≥7 · signals fire on slope, streaks, spikes & volatility`.
6. **Export pack** (`EXPORT PACK` / Fraunces `Share readiness` / `Score, pillars, next steps, and a band-only receipt — shareable artifact.`): buttons `Copy summary` + cyan `Print / PDF`; mini readiness card `HŌMI READINESS` big score + verdict + `NW $0 Runway ∞` + 3 pillar bars + summary + numbered next steps + receipt token (mono, e.g. `homi_rcpt_not_yet_43_…`) + `Educational only — not credit, lending, legal, tax, or investment advice. Band-only receipt · no underlying financials.` Use existing `lib/receipts.ts`.
7. **Financial Reality** (icon + Fraunces `Financial Reality` / `Live gauges from your ledger and banks — runway, DTI, savings rate, and cash flow. Same temperature bands as HōMI production.`): 4 gauge cards — `RUNWAY 5.2 mo / Liquid cash ÷ monthly outflow / WATCH` (amber when <6), `DEBT-TO-INCOME 3% / Debt payments ÷ income (≤28% emerald) / STRONG`, `SAVINGS RATE 46% / Cash flow as % of income (≥20% emerald) / STRONG`, `CASH FLOW $3,033 / Income minus expenses this period / STRONG`. Bands: STRONG emerald / WATCH amber / TIGHT gold-amber. Below: 3 pillar subscore bars `FINANCIAL REALITY 26/35 — DTI 10/10 · Fund 5/8 · Credit 7/7` · `EMOTIONAL TRUTH 23/35 — Stability 6 · Confidence 5 · FOMO check 5` · `PERFECT TIMING 24/30 — Horizon 10/10 · Save 10/10 · DP progress 4/10` (note 35/35/30 — canon pillar maxes; subscore captions from engine insights).
8. **Spend digest** (`SPEND DIGEST` / Fraunces `What changed this week` / `Spend up 609% vs last week — housing drove +$1,850.`): right tile `LAST 7 DAYS $2,688 ↗ +609% vs prior` (crimson when up); left `TOP CATEGORIES` bars (Housing $1,850 (+$1,850), Food $532 (+$532), Debt $220 (+$220), Transport $86 (+$86), Utilities $0 (−$148), Entertainment $0 (−$64)); right `RISING / FALLING` two cols (Up: Housing +$1,850 / Food +$532 / Debt +$220 — Down: Utilities −$148 / Shopping −$119 / Entertainment −$64); `READINESS RECEIPT — Band-only share token — verdict + coarse pillars. No balances, no identity (demo).` + `Issue receipt` button. Empty: `No spend recorded in the last two weeks.` / `No expenses in range.` / `Up None / Down None`.
9. **Add transaction** (card, `Local save` badge): Expense/Income toggle; AMOUNT `$ 0.00`, DATE picker, CATEGORY select (Food default), NOTE `Optional detail`; cyan `+ Add expense`. Below: **Savings goal** card (`Emergency fund`, `Edit`): `$4,800` big + `of $12,000`, gradient progress bar, `40% complete` + `$7,200 to go` (empty: `$0 of $0`, `0% complete`, `Goal reached`).
10. **Spending by category** (donut, total top-right `$3,617`): legend rows with category color dots + amounts + % (Housing $1,850 51%, Food $780 22%, Shopping $329 9%, Debt $220 6%, Entertainment $156 4%, Utilities $148 4%, Transport $86 2%, Health $48 1%). Empty: `Add an expense to see the breakdown.`
11. **Transactions** (`Edit or delete any entry — balances update live`, filter pills All/Income/Expense): rows with direction icon (↗ expense / ↙ income), name, category chip, date, signed amount (crimson − / emerald +), edit + delete icons. Demo rows: Coffee runs Food Aug 1 −$42.00 · Groceries midweek Food Jul 31 −$178.00 · Student loan Debt Jul 30 −$220.00 · Rent Housing Jul 29 −$1,850.00 · Groceries Food Jul 29 −$312.00 · Biweekly paycheck Salary Jul 28 +$6,200.00 · Fuel + transit Transport Jul 27 −$86.00 · Electric + internet Utilities Jul 26 −$148.00 · Consulting weekend Freelance Jul 25 +$450.00 · Streaming + dinner out Entertainment Jul 24 −$64.00 · Household essentials Shopping Jul 23 −$119.00 · Pharmacy Health Jul 22 −$48.00 · Groceries (prior) Food Jul 18 −$248.00 · Amazon haul Shopping Jul 16 −$210.00 · Concert tickets Entertainment Jul 15 −$92.00. Empty: `No transactions yet. Add income or an expense to get started.`
- Refs: readiness-desktop.png (full scroll), readiness-overview.png, prod-final.png, prod-empty.png, prod-empty2.png, audit-overview.png, next-build.png, current-state.png.

## 4. Calendar tab (BudgetCalendar) — the heaviest surface
Header: icon + eyebrow `CASH-FLOW INSTRUMENT` + Fraunces `Decision calendar` + subline (variants: `Projected runway, bills, and ledger on one surface — pay, schedule, and plan without leaving the month.` / `Bills, income, and spend on one timeline — pay, schedule, and plan without leaving the month.`). View toggle pills: `Month` `Week` `Agenda` (+ `Today` button + `Comfort` toggle w/ RotateCcw-ish icon — density toggle).

Month stat tiles (4): `MONTH INCOME $0.00 / 0 ledger events` · `MONTH SPEND $0.00 / Recorded outflows` · `BILLS OPEN $2,345.88 / 6 open · 3 due ≤7d` (gold, w/ progress bar) · `NET CASH FLOW +$0.00 / EO M projected $16,374.72` (cyan/emerald signed). (Agenda-variant labels: `BILLS STILL OPEN … 6 open`.)

Projected cash runway card: `PROJECTED CASH RUNWAY` + Fraunces month `August 2026` + right `BANK CASH NOW $18,720.60`; area chart (cyan gradient) of projected cash across the month + daily-net bars; legend `— Projected cash ▪ Daily net` + hint `Click a bar to jump · arrows navigate · T today`. Filter chips right (or below on mobile): `All 6` (active cyan) `Bills 6` `Income 0` `Spend 0`.

**Month grid**: `‹ August 2026 ›` nav arrows; SUN–SAT columns; trailing/leading days dimmed; day cells rounded with:
- today = cyan filled number circle; selected = cyan border; weekend chip `WEEKEND` in inspector.
- bill chips (truncated name, colored by state: gold bg = due soon e.g. `Apartment…`, `Spectrum i…`, `Student loan`, `Mobile plan`; emerald/teal = paid or today e.g. `TECO elect…`, `Netflix`), dots under days: crimson = overdue, gold = bill due, cyan = spend, emerald = income/paid.
- spend/income aggregates on cells (e.g. `-$155`, `Food +1`, `-$1.9k`).
Legend: `• Overdue • Bill due • Spend • Income / paid` + `M month · W week · A agenda · arrows move · T today` (keyboard nav required).

**Week view** (`Aug 2 – Aug 8` header + arrows): 7 tall day columns; each shows weekday + date + net (`-+$112`, `Clear`, `-+$1.9k`) + bill chips (`T… -$112`, `A… -$1.9k`, `Sp… -$80`). Selected column cyan border.

**Agenda view** (`Next 21 days` / `Next 28 days` list): day rows — dot, `Sun, Aug 2` + right `Today`/`In 3d` + `Bills $112.40` badge; bill line w/ icon + name + state chip (`DUE` gold-outline / `UPCOMING` cyan / `SCHEDULED` dim) + amount right. Selected row highlighted.

**Selected-day inspector** (right rail): `SELECTED DAY` + Fraunces `Mon, Aug 10` + chips (`IN 8D` / `TODAY` / `WEEKEND`) + `DAY IMPACT -$15.49` (crimson negative) + `Proj $16,662.72`; tiles `IN $0.00` (emerald) `OUT $154.90` `BILLS $15.49` (gold); `DAY PRESSURE $15.49` thin bar; `PAY FROM` account dropdown (`Total Checking · $4,280.42`); buttons `+ Add bill` (cyan) / `Log spend` (or `✕ Close` when form open).
- Add-bill form: NAME `e.g. Car payment`, AMOUNT `0.00`, CATEGORY select (Utilities), cyan `Save bill on Aug 7`.
- Log-spend form: NOTE `What did you spend on?`, AMOUNT, CATEGORY (Food), cyan `Log spend on Aug 10`; success line emerald `✓ Expense logged on this day.`
- `BILLS · n` list: bill card (name + chip SCHEDULED/DUE/UPCOMING/PAID-emerald + `Entertainment · Autopay · monthly` + amount) with cyan `Pay now` + `Schedule` buttons.
- `LEDGER · n` list: rows (dot, `Food` / `Food · manual` −$42.50; `Bill pay · TECO electric` / `Utilities · bill-pay` −$112.40); empty: dashed box `No ledger entries on this day`.
- `UPCOMING · n`: `TECO electric Aug 2 · due today $112.40`, `Apartment rent Aug 5 · in 3d $1,850.00`, `Spectrum internet Aug 7 · in 5d $79.99`, `Netflix Aug 10 · in 8d $15.49`, `Student loan Aug 14 · in 12d $220.00`, `Mobile plan Aug 20 · in 18d $68.00`.
- Refs: cal-final-agenda.png, cal-final-month.png, cal-final-week.png, cal-final-inspector.png, cal-final-mobile.png, cal-final-verify.png, cal-ultra-*.png (premium spacing variant), cal-premium-*.png, cal-week-fix.png, cal-mobile-top.png, budget-calendar.png, budget-calendar-detail.png, budget-calendar-mobile.png.

## 5. Banks & bills tab (BankingCommand)
- Header card: icon + `BANKS & BILLS` + Fraunces `Live cash command` + `Linked balances drive bill pay and closed-loop readiness.` Tiles: `CASH $18,572.60` · `ACCOUNTS 3` · `BILLS OPEN $2,345.88` (gold) · `OVERDUE 0`. Right panel: `▤ Paying a bill updates ledger, cash, score, path, and signals.` + `Last sync: Aug 3, 2:04 PM` + progress bar + `Balances you enter drive bill pay. Open banking can replace entry when connected.` Demo-mode cyan progress bar + note `Demo open-banking — production swaps in Plaid / MX credentials.`
- **Linked accounts** card (`Pull live balances for bill pay. Demo open-banking flow — swap in Plaid / MX / Finicity for production credentials.`): `Sync now` + cyan `Link bank` buttons. Tiles `CASH ON HAND $18,572.60` · `CREDIT UTILIZED $0.00` (gold) · `LAST SYNC Aug 2 · 2:30 PM`. Account rows: institution badge (2-letter monogram on brand-tinted square: CH cyan Chase, AL purple Ally), name, `Chase · checking · ••••4821`, balance right + `$3,972.00 avail.` + link icon. Demo accounts: Total Checking $4,132.42 · Savings $11,240.18 · Online Savings (Ally) $3,200.00.
- Self-entry variant (no linked banks): `ACCOUNTS — Cash you control` / `Add balances you trust. They drive bill pay, runway, and the closed loop — educational readiness only.` + `Refresh` + cyan `Add account`; success line `Account added. Balances power bill pay and readiness.`; row `Chase checking · Chase ··0000 · checking $5,000.00`.
- **Bill pay** card (`Pay or schedule from live account balances — each pay closes the score loop.` + `+ Add bill`): tiles `OPEN $2,346` · `DUE TODAY 1` · `OVERDUE 0`. Emerald success banner after pay: `✓ Bill paid. Ledger, cash, path & readiness updated.` Bill cards: `TECO electric DUE TODAY $112.40` / `Utilities · due Aug 2 · Chase ••4821` + pay-from dropdown (`Total Checking · $4,132.42`) + cyan `Pay now` + `Schedule` + `Remove` (dim). `Apartment rent UPCOMING $1,850.00` / `Housing · due Aug 5 …`. Empty: dashed box `All bills paid — closed loop is clear.`
- Refs: budget-banking.png, budget-banking-after.png, budget-banking-mobile.png, loop-pay.png, prod-account.png.

## 6. Wealth tab (BrokerPanel + PortfolioPanel + NetWorthPanel)
- Brokerage feeds: demo broker link (Fidelity 401k), broker rows w/ metadata (Fidelity/Vanguard/Schwab/E*TRADE/Robinhood), sync. `Demo brokerage — production swaps in SnapTrade / Plaid Investments.` (planner/brokers.ts metadata).
- Investment portfolio: marked prices note, tiles (market value, cost basis, unrealized gain emerald/crimson signed, day change), allocation donut, holdings table (ticker, shares, price, value, gain): FXAIX / VTI / VOO / BND / VXUS / AAPL / MSFT. `Mark prices` refresh.
- Net worth stack: assets vs liabilities tiles, `NET WORTH $72,098.56`, other-assets add (home, vehicle…), debts list w/ balances/APR/min payment.
- Refs: budget-wealth-overview.png, budget-wealth-added.png, budget-wealth-mobile.png, budget-broker.png, audit-wealth.png.

## 7. Plan tab (PlanCommand) — `PLAN LAB`
- Header: `PLAN LAB` + Fraunces `Decision readiness tools` + `Path, housing, debt, dual score, Monte Carlo — every move can pulse the HōMI-Score.` Sub-tab pills: `Path` (BookOpen) · `Housing` (Home) · `Debt` (Scale) · `Household` (Users) · `Models` (LineChart) · `Share` (Sparkles).
- **Path**: `PATH TO READY` + Fraunces `Turn live numbers into sequenced moves`; generate/regenerate/clear; binding-constraint sequence cards w/ due dates + reasons; completion progress; optional step `Optional 90-day readiness review`; states: complete (`Path steps complete — reassess when life moves`), first-move (`Generate a protective sequence from live numbers — One binding constraint at a time — not a checklist wall.` + `Generate Path`). Engine = CANON `lib/path.ts` (never Grok's 75/55 scorer).
- **Housing** (rent vs buy): `HOUSING LENS` + Fraunces `Rent vs buy` + verdict chip right (`STRETCH` gold / others per engine: blocked / buy_stretch / rent_clearer / buy_competitive) + `CFM-backed monthly and 5-year view. Magnitude only — not a buy recommendation.` + sentence `Buy is possible but stretched. Know the monthly price of the stretch.` Tiles: `BUY / MO $2,969` (cyan) · `RENT / MO $1,850` · `MONTHLY DELTA +$1,119` (gold) · `HOUSING RATIO 45%` (gold) · `5-YR BUY CASH $178,154` · `5-YR RENT CASH $111,000` · `EST. EQUITY @ 5Y $130,417` (emerald). Line: `Rough cash/equity break-even ~ 0.1 years · gap to 20% down $47,000` (gap in gold). Inputs (2-col): TARGET PRICE 425000 · DOWN PAYMENT 38000 · RATE % 6.5 · CURRENT RENT 1850 · HOA / MO 45 · TERM YEARS. Warnings (⚖ icon): `Housing ratio above 36% — elevated stretch.` `Buy costs $1,119 more per month than current rent.` `$47,000 still needed to reach a 20% down payment.`
- **Debt**: avalanche vs snowball comparison via `lib/tools/debt.ts` (payoff order, interest saved, month curves, extra-payment input).
- **Household**: dual readiness — partner profile (income share, stress), per-member + joint score (joint = min, hard-stop union) via `lib/household.ts`; `Align with your partner on one number` copy.
- **Models**: Monte Carlo — P10/P50/P90 bands, `2,000 runs` note (Grok UI says 2000; engine default stays canon 10000 — UI may pass 2000 explicitly), survival/distress %, `Re-roll seed`; Decision rehearsal — buy now $88,655 vs wait-12 −$28,326 vs wait-24 −$22,309 via `lib/rehearsal.ts`.
- **Share**: export pack + receipt (same as Overview §3.6).
- Refs: budget-plan.png, budget-path.png, budget-plan-mobile.png, loop-plan-housing.png, readiness-plan.png.

## 8. Closed-loop ImpactToast (global, bottom-right)
Card slides in: `CLOSED LOOP · BILL PAID` / `CLOSED LOOP · PATH STEP COMPLETED` (icon + ×). Body variants:
- `73 → 73` big mono + `0.0` delta + `Score held steady. Path or cash position may still have moved.`
- `Path steps complete` + `100% path complete` + `"Optional 90-day readiness review"` + `Protective homework on this path is clear. Path steps complete. Reassess when life inputs change — not yet is not no.` + `→ Path steps complete. Reassess when life inputs change — not yet is not no.`
Engine: planner/closed-loop.ts + impact.ts (snapshot score before mutation → emit after). Refs: audit-toast.png, loop-overview.png, loop-pay.png, loop-mobile.png, next-closed-loop.png, next-mobile-loop.png, next-after-gen.png, next-no-mark.png.

## 9. States matrix
- **Demo loaded** (default first visit): score 73 ALMOST, pillars 74/66/80, income $6,650, spent $3,617, saved 46%, bank cash $18,720.60, portfolio $57,077.95, net worth $72,098.56, bills open $2,493.88, runway 5.2 mo, DTI 3%; 1–2 overdue-bill signals + no-path signal; `Reset demo` button.
- **After bill pay**: paid banner, bill PAID chip, ledger entry `Bill pay · …`, balances update, closed-loop toast.
- **After path generate**: path strip ACTIVE, signal clears, toast on step completion; `100%` completion state.
- **Empty/cleared** (`Clear data`): score 43 NOT YET + HARD-STOP (demo credit profile), all tiles $0, runway ∞, START HONEST 4-step, `Clear data` button, protection signals.
- **Error/debug**: error-debug.png shows a dev error overlay from Grok's build — ignore; it's their tooling artifact, not a spec.
- Mobile: single column, tabs wrap, tiles 2-col, inspector stacks below grid, bottom strip. Refs: budget-mobile.png, cal-final-mobile.png, cal-premium-mobile-final.png, readiness-mobile.png, prod-mobile.png, next-mobile.png, loop-mobile.png.
