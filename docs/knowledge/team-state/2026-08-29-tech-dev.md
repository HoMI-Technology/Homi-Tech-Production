# HoMI Tech Dev — state packet 2026-08-29

## Locks / HOLDs (and why)

- **Packet 2 parked.** No live auto-score, no Apply-to-my-score, no second ledger/score write, no 45-q shortcut.
- **Scoring engine is not this loop.** Do not touch WEIGHTS, 45-q, hard-stop defs. Score writes only through a completed 45-q. Chrome/IA only.
- **#333 closed, not merged.** Empty first-viewport poster / still `4b5d47ad` is dead. Do not revive. Do not match Brand’s empty still.
- **No production deploy from the muster loop.** Do not `vercel --prod`. Production alias already flipped; this packet does not ship anything.
- **Local `npm run build` fails by design.** CI (verify + e2e) is the proof. Do not treat a local build red as a merge block or as a reason to patch.
- **After this muster: Baseline 001 fold work is sequenced Brand direction → App UX → Dev.** Dev implements only on branch `redesign/threshold-fold-baseline001`. **No fold PR without before/after vs `docs/design/baseline/`.** Founder already called Baseline 001 horrible; it is the floor, not the target.
- **Plaid Identity:** production-approved, but no Identity PR until Product names a use. Do not wire Identity into scoring or the ledger. Do not put the Plaid client ID in the repo or UI.
- **Nav lock:** first screen is **HōMI**, not Home. Rail: HōMI + Assess + More. Money is depth/evidence, not a peer home. No `/advisor` CTA on the fold. One repo `ThresholdCompass` (viewBox `0 0 200 200`, r 85/60/35, yellow keyhole). Never generate a compass. Never a second compass FAB.
- CEO 2026-08-29: KEEP Personal Assistant off `docs/launch/OWNER-PUNCH-LIST.md`. Dev keeps that markdown in the repo. Assistant surfaces founder-only items in the company chat at launch.

## Decisions made (my domain)

- **#334 KEEP** squash `524c5423` — public routes no longer `await getUser()` in middleware (504 MIDDLEWARE_INVOCATION_TIMEOUT). Remaining auth lookup bounded 2s; fail-closed on protected, fail-open on retired `/results`.
- **#335 KEEP** Path-led `/dashboard` unification (Money folded in, not a standalone dashboard). Then **#336 KEEP** AppSidebar HōMI rail (pulse/Held/duplicate 61 killed). Then **#337 KEEP** compass-as-page first screen. Feature `6ce5ec1`, squash `c1727cd9` at 5:41 PM ET after Founder GO + verify+e2e. Dev merged #334 and #337. Org also merged #335/#336/#338/#339.
- **#333 closed** at founder ask. Superseded. Not merged.
- Live tip at muster inspect (8:39 PM ET): **main `e75f453` (#339 knowledge muster)**. Production deploy `6161700906` on that SHA. Earlier Production on `c1727cd9` (#337) at 5:43 PM ET did flip.
- Ground-up fold is **ThresholdFold** + repo `ThresholdCompass` as the page. Last `AssessmentResult` wins (numeral, runway). Empty = Assess + em dash. No celebrate on DNP. No 80–100 legend.

## In-flight (branch / PR / wave)

- **No open PRs** as of this packet.
- **Next fold work is not started.** Wait Brand direction, then App UX brief, then implement on `redesign/threshold-fold-baseline001` with before/after vs `docs/design/baseline/` (starting `2026-08-29-verdict-screen-BASELINE.png`). Do not open that PR from this muster.
- Cloud agents used this day: `bc-8e334538` (#334), `bc-6538b45d` (#335), `bc-288d9516` (#336), `bc-d13e4935` (#337). None should keep patching the card stack.

## Top gotchas

- Two fleets must not iterate ThresholdFold in opposite directions. Baseline README already flags this. Next fold PR must cite the 8 Baseline 001 defects (node-vs-61 collision, bare `CASH —`, ALL-CAPS hard-stop, cryptic Hot, crimson numeral, dead space, red edge-glow, unexplained 61 vs DNP) and show pixels against the baseline PNG.
- Production alias can lag git main. QA preview SHAs, not homitechnology.com, until the alias is the intended SHA.
- Do not Brand-score a moving HEAD. Ping only a settled SHA.
- Do not mix middleware, Identity, Packet 2, or admin visual rewrite into a fold PR.
- Do not clone the repo onto a local machine for this work. Cloud agent / `gh` only.
- `HomeFold` card stack is not the first screen anymore. Hit ThresholdFold / compass-as-page.

## Needs from other agents

- **Brand:** direction on Baseline 001 (the founder-rejected verdict screen). QA crop is not a ship gate. Do not invent a replacement poster.
- **App UX:** signed-in first-screen brief after Brand, for branch `redesign/threshold-fold-baseline001`.
- **Assistant / Knowledge:** keep this packet as Dev SoT for 2026-08-29. Newer packet wins.
- **Finance:** Path + hard stops still lead; 0.5 mo on the compass is last AssessmentResult, not a live score. No engine change requested.
- **CEO:** no merge/deploy from this muster. Next fold PR stays draft until Brand + App UX + verify+e2e + founder GO.
