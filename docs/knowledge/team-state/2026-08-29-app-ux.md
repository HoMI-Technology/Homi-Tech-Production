# HoMI App UX — state packet 2026-08-29

## Locks / HOLDs (and why)

- Packet 2 PARKED. No live auto-score. No Apply-to-my-score.
- #333 CLOSED superseded 29 Aug 4:02 PM ET (`9f8f7ce`, not merged). Empty still `4b5d47ad` is not ship law. Do not revive. Do not match. Do not invent a replacement poster. HOLD was “do not ship that look,” not “PR still open.” README standing “App UX carries HOLD on #333” is stale.
- Surgical W1 REJECTED. Unification W1 stays rejected on sight.
- Finance: Path + hard stops lead. Compact ScoreRail = last AssessmentResult.score only, verdict once (never NOT_YET), temperature on, never 80–100, never closer-to. Money MAY appear as Path-evidence strip (runway/liquid/flags). 4xl surplus / 4-tile wall forbidden. This supersedes morning UX “money off Home under DNP.”
- Brand: compass in SHELL not ScoreRail. Kill PillarRings on compact. First-screen name HōMI. QA still is crop only, not a gate.
- Wave 1 prompt: “Your money picture changed. Re-check readiness?” Retake → /assessment. Not now = this band-cross. Moved/when-you-want is dead.
- No 0d chrome. Omit age and day-30 when held OR last-read <30d. `{score} · from {Mon D}` on a 0-day hold is dead.
- Never deploy production. App UX does not open PRs / undraft / merge.

## Decisions made (my domain)

- One fold: hard stop + Path only primary + one compact score instrument.
- Money folded into /dashboard, not a standalone dashboard.
- IA PASS on `9f8f7ce` void for ship with #333 close.
- Merged into Product ~1:17 PM ET; this muster restores App UX for pixels vs baseline. Product still owns briefs. App UX will not brief Dev.

## In-flight (branch / PR / wave)

- This agent: no branch, no PR, no routines. Packet first.
- #336 MERGED `6f72d10`. #337 Knowledge live stamp `c1727cd9`. Do not restamp Production from git HEAD.
- Next after Brand signs direction: Baseline 001 ThresholdFold 8 defects vs `2026-08-29-verdict-screen-BASELINE.png`. Branch `redesign/threshold-fold-baseline001`. Founder’s rejected fold is ThresholdFold (#335–#337), not legacy HomeFold.

## Top gotchas

- `4b5d47ad` is a still hash, not a git SHA.
- Three ThresholdCompass files; do not fix the dead finance duplicate.
- Center pip vs 61 is defect 1; never redraw the mark.
- Two fleets, one fold: do not iterate closed HomeFold still vs ThresholdFold.
- Path pill client-hydrates. Companion leftover below fold only, one sentence, no ledger writes.

## Needs from other agents

- Brand: sign Baseline 001 8-defect direction before App UX builds.
- Dev: do not revive #333. Do not deploy production.
- Finance: do not reopen WEIGHTS / 45-q.
- Product: owns briefs.
- Knowledge: correct README HOLD-on-#333 standing.
