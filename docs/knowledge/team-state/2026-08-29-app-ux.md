# HoMI App UX — state packet 2026-08-29

## Locks / HOLDs (and why)

1. Packet 2 PARKED — no live auto-score, no Apply-to-my-score. Score writes only through 45-q.
2. #333 look HOLD — do not revive, do not match still `4b5d47ad`, do not invent a replacement poster. Founder rejected that empty first viewport. GitHub: #333 CLOSED superseded 29 Aug 4:02 PM ET (`9f8f7ce`, mergedAt null). App UX does NOT carry an open-PR HOLD. README “App UX carries Product HOLD on #333” is stale.
3. Surgical W1 REJECTED — DASHBOARD_UNIFICATION_W1.md stays rejected on sight. Founder skipped Brand-still wait; ground-up ThresholdFold is the floor.
4. Never deploy production. Never push/merge to main directly (branches+PRs; CI verify is merge gate). Never spend money or post externally.
5. No ThresholdFold build until Brand signs direction AND before/after pixels exist. Baseline PNG already failed review.
6. local npm run build fails BY DESIGN — never touch SWC.
7. Never soften a failing test assertion.
8. Finance: Path + hard stops lead; last AssessmentResult.score only; never print 80–100; never closer-to; money strip = Path evidence only (runway/liquid/flags); 4xl surplus forbidden.
9. Brand: compass in SHELL not ScoreRail; kill PillarRings on compact; first-screen name HōMI.
10. No 0d chrome — omit age and day-30 when held OR last-read <30d. `{score} · from {Mon D}` on a 0-day hold is dead.

HOLDs are law until CEO lifts them.

## Decisions made (my domain)

- One fold: hard stop + Path only primary + one compact score instrument.
- Money folded into /dashboard as Path evidence, not a standalone dashboard.
- IA PASS on `9f8f7ce` void for ship with #333 close.
- Wave 1 prompt stays: “Your money picture changed. Re-check readiness?” Retake → /assessment. Moved/when-you-want is dead.
- App UX owns ThresholdFold defect fixes once Brand signs direction. Product owns briefs. App UX will not brief Dev.
- App UX does not open PRs / undraft / merge. If a branch is needed later: redesign/threshold-fold-baseline001, draft PR, CI gate, never main.

## In-flight (branch / PR / wave)

- This agent: no branch, no PR, no routines. Waiting on Brand direction.
- #333 CLOSED superseded. Stay out.
- #336 MERGED `6f72d10`. #337 Knowledge live stamp `c1727cd9`. Do not restamp Production from git HEAD. origin/main now `e75f453` (#339).
- Baseline 001 not started.

## Top gotchas

- `4b5d47ad` is a Brand still SHA256, not a git SHA.
- Three ThresholdCompass files; do not fix the dead finance duplicate (still says HōMI-Score).
- Center pip vs 61 is defect 1; never redraw the mark.
- Two fleets, one fold: do not iterate closed HomeFold still vs ThresholdFold.
- Path pill client-hydrates. Companion leftover below fold only.
- Pixels decide. “The code is better” is not evidence.

## Needs from other agents

- Brand: sign direction on Baseline 001’s 8 defects vs `2026-08-29-verdict-screen-BASELINE.png` before App UX builds.
- Dev: do not revive #333. Do not deploy production. Do not push main.
- Finance: do not reopen WEIGHTS / 45-q / hard-stop defs.
- Product: owns briefs.
- Knowledge: correct README standing that App UX carries an open HOLD on #333.
- CEO: only person who lifts HOLDs.
