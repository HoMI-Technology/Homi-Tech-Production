# COMPARE_CRAFT v4 — 2026-09-11

**CLEAR:** Product PR H + Finance PR H (this floor)  
**Mocks:** `docs/design/brand-shell-craft/` (`compare-empty-v4`,
`compare-hard-stop-v4`, `compare-normal-v4`) — orientation only, **≠ PIXEL**.
Mock copy must not ship as Production invent $ / a second score.  
**ADR:** `docs/adr/006-signed-in-product-v4-retires-operate-chrome.md`  
**Flag:** same `HOMI_V4_HOME_ENABLED` / `isV4RouteActivated`. No second flag.

Signed-in Compare workspace on **`/scenarios`**, rail label **Compare**, inside
Shell v4 `main#main`. Stay draft. Do not undraft, merge, or deploy Production.

## Locked

- Route is `/scenarios` only (already V4_PENDING). Optional `/compare` alias
  redirects here — not a second workspace. Primary rail Compare is selected
  (lift + **2px cyan edge**, no glow, no cyan pill). Compass stays in the shell
  rail, never on the fold. Mobile: Compare lives under **More**.
- Educational scenarios **REUSE_ENGINE**. Adapt the picture into Shell v4. Do
  not rewrite `lib/readiness/scenario*.ts` or `lib/tools/scenarios.ts`. Do not
  invent results.
- States: empty (Start a comparison, no fake results) · hard-stop ACTIVE
  (educational-only, empty-or-live, never On track / READY) · normal ≤3–4
  approved cards · stale / error honesty with **quiet age**.
- Compare never writes official score. AssessmentResult stays Home/Assess SSOT.
  Hard-stop chrome is last-read only. Templates do not override the hold.
- Ask placeholder: **Ask HōMI about this comparison...** (top command + right
  column). Right HōMI is a **column**, not a floating overlay.
- JetBrains Mono for **live SSOT numbers only** — never for empty copy, labels,
  or a second score numeral. No verbose fixture meta in the fold (raise the bar
  vs Money's craft-figures age line).
- Fixture stills (`HOMI_V4_VISUAL_FIXTURE`) stay Preview-only. Approved-template
  cards are orientation — live saved rows win at Pixel.

## Kill list

Second official score · invent $ · On track under hard stop · glow/pill
selected · floating Clarity · invent SKUs · compass on fold · FI v2 / WEIGHTS ·
Homie cast · large cyan pills

## Soft nits (P2)

Mock pixel-match vs brand-shell-craft stills. Do not block the stay-draft.
Raise the bar vs Money on selected edge, Ask copy, HōMI column, quiet age, and
no fixture meta in the fold.
