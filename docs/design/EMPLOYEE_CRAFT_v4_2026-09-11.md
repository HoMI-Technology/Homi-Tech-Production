# EMPLOYEE_CRAFT v4 — 2026-09-11

**CLEAR:** Product PR K + Finance PR K (this floor)  
**Mocks:** `docs/design/brand-shell-craft/` (`employee-empty-v4`,
`employee-hard-stop-v4`, `employee-normal-v4`) — orientation only, **≠ PIXEL**.
Mock copy must not ship as Production invent $ / a second score / craft-badge
fold prose.  
**ADR:** `docs/adr/006-signed-in-product-v4-retires-operate-chrome.md`  
**Flag:** same `HOMI_V4_HOME_ENABLED` / `isV4RouteActivated`. No second flag.

Signed-in Employee operate home is a thin Shell v4 destination at
`/employee/dashboard`. One shell · different jobs. Stay draft. Do not undraft,
merge, or deploy Production. K2–K4 (`/partner` `/admin` `/team`) stay closed.

## Locked

- Route is `/employee/dashboard` only (V4_PENDING via `/employee` prefix).
- Empty or live operate SSOT only. Never invent teammate lists, scores, or $.
  Never write AssessmentResult.
- Hard stop never On track. Compass shell-only. No Homie. Free === Pro.
- Desktop: right HōMI column (not overlay). Micro Clarity header — not both large
  wordmark + CLARITY.
- Mobile: Ask sheet = one Ask only (workspace prompts, no nav dump). Column Ask
  hidden below 1280.
- ≤3 primary fold blocks. Selected rail = 2px cyan edge, no glow, no pill.
- Quiet Synced/Stale age. JetBrains only for live SSOT $.
- Workspace jobs: Home · Attention · Privacy. No new `/employee/*` URL sprawl.
- No craft-mock fold prose in Production (`Switch workspace only if >1` stays
  craft-only).

## Ask placeholder (workspace-bound)

| Surface | Placeholder |
| --- | --- |
| Employee | Ask HōMI about this workspace... |

## Prompts (≤3, deep-link only)

**Empty:** Why no peer scores? · Privacy on this workspace  
**Hard-stop:** What does this hold mean here? · Open personal Path · Why no READY theater  
**Normal:** What is live here? · When does age go stale? · Why no HeroScore

## Kill list

HeroScore · peer-score · invent $ · On track under hard stop · Stand/Plan/Decide/More
role labels · Homie · craft-mock fold prose · floating overlay · Partner/Admin/Team
reopen · FI v2 / WEIGHTS

## Soft nits (P2)

Mock pixel-match vs brand-shell-craft stills. Do not block the stay-draft.
Raise the bar vs PR J on micro Clarity, one mobile Ask, 2px cyan selected, and
no personal Home clone.
