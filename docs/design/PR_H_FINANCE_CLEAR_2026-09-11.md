# PR H — Compare v4 finance CLEAR — 2026-09-11

**Status:** CLEAR  
**PR:** stay-draft PR H

## GATE

Score / verdict / hard stops on Compare come from **AssessmentResult** (last-read
row). Compare sequences educational what-ifs. It does not mint a second score,
does not write AssessmentResult, and does not clear a hard stop.

## Locked

- Empty or live SSOT only. Do not invent balances, net-position theater, ~pts,
  or a second official verdict.
- Reuse `lib/readiness/scenario*.ts` and `lib/tools/scenarios.ts`. Do not
  rewrite simulation math, CFM, or ledger. Do not quote
  `DEFAULT_SIMULATION_INPUTS` as Production results.
- Approved templates are educational cards (labels + follow). Live saved
  `tool_scenarios` rows may appear as cards. JetBrains Mono only for live SSOT
  numbers — never for empty copy, labels, or a second score numeral.
- Hard stop outranks. Exploring templates does not clear the hold. Empty-or-live
  only — never On track / READY theater on this page.
- Free === Pro on the last read. Do not rewrite `lib/scoring/*`.

## OUT

Invent $ · second official score · On track under hard stop · WEIGHTS / Packet2 /
FI v2 · guest official score · `supabase db push`
