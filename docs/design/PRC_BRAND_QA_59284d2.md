# PR C Brand QA — CODE FAIL @ `59284d2`

**Status:** CODE FAIL at `59284d2` → fix-forward on stay-draft #391  
**Tip:** `cc84ac6` (CCP #390)  
**Pixel Gate / flag:** unchanged. `HOMI_V4_HOME_ENABLED` default **false**. Stay **DRAFT**.

## Verdict at `59284d2`

Brand CODE FAIL. Primary rail, Wordmark/Compass, Finance GATE, hard-stop
honesty, flag-false, and half-shell ban **PASS**. Four P0 chrome/home
defects blocked CODE PASS.

## P0 — must-fix (this revision)

| # | Defect @ `59284d2` | Fix |
| --- | --- | --- |
| 1 | Secondary was Tools · Settings · Support | Secondary **Bills · Tools · Learn**. System **Accounts · Settings**. Support dropped (no proven `/trust` product route). |
| 2 | `.left-rail-link.is-active` cyan wash/pill | V4 uses `.v4-rail-link.is-active`: surface lift + **white** label + **~2px cyan edge** (mobile tab: top edge). Invent-chrome left-rail CSS untouched. |
| 3 | Assess not a solid cyan command action; no Ask HōMI field | Assess = one solid cyan top action. **Ask HōMI** field in the command bar. Assess stays off the primary rail. |
| 4 | Key status soup + What’s next checklist as peers | Home hierarchy: Decision context → Readiness hero → Decision evidence (**three pillars**) → Current Path step → Money evidence → What changed → Contextual tools (~3–4). |

## P1 — cheap nits folded

- Eye-path: numeral → verdict → hold → action
- Drop `/ 100` on the score plate (aria still says out of 100)
- Crimson on **DO NOT PROCEED** words only
- Path CTA `btn-ghost` / no cyan glow
- Rail width **222** (216–228)
- Personal foot chip on the left rail

## KEEP (still PASS)

Primary Home · Money · Path · Compare · Wordmark / ThresholdCompass ·
Finance GATE (AssessmentResult only) · hard-stop honesty · flag false ·
half-shell ban · post-login `/` until the flag is exact `"true"`.

## OUT

Undraft · merge · flag-true · Pixel Gate skip · invent $ · On track under
hard stop · Homie cast · `/dashboard` as Home.
