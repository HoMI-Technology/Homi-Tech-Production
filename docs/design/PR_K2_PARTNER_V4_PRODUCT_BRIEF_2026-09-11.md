# PR K2 — Partner v4 product brief — 2026-09-11

**Status:** CLEAR  
**PR:** stay-draft PR K2 — Partner. Do **not** undraft. K3–K4 out.  
**Section:** 6 — Household / B2B / Org (`/partner/dashboard`) plus declared Shell v4
dependency (Section 2). Invite stays live `#355` `/first-moment?ref=`. Do not
edit Scoring (Section 0). Do not reopen `/admin` `/team`.

## Decision

Product CLEAR for Partner operate home v4 craft. Implementation may continue on
the stay-draft PR. This is **not** APPROVE VISUAL DIRECTION, not merge, and not a
second flag. Raise the bar vs Employee: empty primary CTA is **Invite** (not a
Clarity question). SITE_URL fail-loud. Shadow-score invite stays dead.

## Locked

1. Add **`/partner`** to `V4_PENDING_PATHS` (prefix covers `/partner/dashboard` + depth).
2. Shell v4 Partner operate home at **`/partner/dashboard`** — portals redirect here;
   do not invent a peer `/partner` home.
3. Invite: live `#355` / `/first-moment?ref=` KEEP — **never** `/shadow-score?ref=`.
   SITE_URL fail-loud keep. Do not invent a new Production URL. Guest invite is not
   a role home — do not shove `/first-moment` into V4_PENDING.
4. Book pulse from live `referral_source` / SSOT only — never invent a client list or $.
5. Never invent score · no HeroScore · no partner-written AssessmentResult · no
   client score override.
6. Empty primary CTA = **Invite** (not a Clarity question). Headline: **No clients in
   your book yet.** Ask: *Ask HōMI about this partner book…*
7. Micro Clarity column · 2px cyan selected · no glow/pill · no float overlay.
8. Same flag dual gate (`HOMI_V4_HOME_ENABLED`). No second flag. Keep #397 client
   post-login law. Do **not** reopen `/admin` `/team` (K3–K4).

## OUT

Undraft / merge / Production deploy · second flag · HeroScore · shadow-score invite
restore · silent bad SITE_URL · invent $ / fake book · On track on a held client ·
Clarity-as-CTA · Homie · craft-mock fold prose · Admin/Team reopen · Assessment /
Path / Money / Compare / Employee / system-surface rewrites · FI v2 / WEIGHTS ·
Pixel Gate skip
